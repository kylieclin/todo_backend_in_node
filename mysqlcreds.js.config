const creds = {
    host     : 'host',
    user     : 'user',
    password : 'password',
    database : 'database',
    port: port
}

module.exports = creds;

//config 檔案可以直接把config刪除後就是一個js file 一個cred的樣本